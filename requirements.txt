# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Database
sqlalchemy>=2.0.0
# alembic>=1.14.0          # bật khi schema ổn định, xem docs/decisions/
psycopg2-binary>=2.9.0

# Xử lý ảnh — tước EXIF, nén, làm mờ khuôn mặt, băm tri giác (pHash)
pillow>=11.0.0
imagehash>=4.3.0
opencv-python-headless>=4.10.0

# Upload file cho FastAPI
python-multipart>=0.0.20

# Xác thực — tự làm auth thay vì Supabase, xem ADR-0004
pyjwt>=2.10.0

# Tầng T0.5 — CLIP zero-shot: xem requirements-local-model.txt.
# torch chiếm 1,19 GB nên KHÔNG nằm ở đây: gói miễn phí của Render không đủ
# dung lượng lẫn RAM. Bản deploy chạy LOCAL_MODEL_ENABLED=false.

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
